from setuptools import setup, find_packages

setup(
    name="Lib_Gamma",
    version="0.1",
    packages=["Lib_Gamma"], # Specifichiamo direttamente il nome della cartella
    install_requires=[],
)
