from setuptools import setup, find_packages

setup(
    name="Lib_Gamma", # Il nome che userai per importarla
    version="0.1",
    packages=find_packages(),
    install_requires=[], # Qui metterai eventuali altre librerie da cui dipendi
)
